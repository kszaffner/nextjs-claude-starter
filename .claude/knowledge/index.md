# Knowledge Index

Status: **enabled**

Index-first: check this file for relevant topics before loading any file
below in full. See `.claude/skills/knowledge/SKILL.md` for the retrieval
and update workflow, and `docs/knowledge.md` for the design rationale.

This starter ships with the Knowledge Base already initialized (empty
beyond this index) so `/knowledge` commands have somewhere to write to
immediately. Delete `.claude/knowledge/` entirely if you don't want to use
this system in a given project — the rest of the starter works without
it.

## Topics

_No topics recorded yet. Run `/knowledge update` after a task that made a
non-obvious decision, or add entries directly._

<!--
Example entry once you have real content:

- [products.md](features/products.md) — why product categories are a
  closed enum, not free text
-->
