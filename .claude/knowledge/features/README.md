# features/

Non-obvious business rules and assumptions behind a specific feature/
module — the "why does this behave this way" that isn't visible just by
reading the code (e.g. why a discount can't stack, why an order can't be
edited after a certain status). One file per feature/module topic, named
after the module (e.g. `products.md`, `checkout.md`).
